#include <bits/stdc++.h>
using namespace std;

int main()
{
    char ch;
    cin >> ch;
    if (ch == 'z')
    cout << 'a';
    else { 
    
    int num = (int)ch;
    cout << char(num+1);
    }
   
}